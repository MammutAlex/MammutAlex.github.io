{
  "name": "mamutalex_landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "core-js": "^3.6.5",
    "nuxt": "^2.14.6",
    "@nuxtjs/color-mode": "^2.0.0",
    "@nuxtjs/tailwindcss": "^3.1.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
